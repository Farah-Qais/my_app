<%= form_with(model: hose) do |form| %>
  <% if hose.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hose.errors.count, "error") %> prohibited this hose from being saved:</h2>

      <ul>
        <% hose.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.select :user_id, User.all.pluck(:name, :id)%>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.label :size, style: "display: block" %>
    <%= form.number_field :size %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
